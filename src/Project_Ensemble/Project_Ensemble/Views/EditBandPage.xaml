<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Project_Ensemble.Views.EditBandPage">

    <StackLayout VerticalOptions="Center">
        <ActivityIndicator IsVisible="{Binding IsBusy}"
                               IsRunning="{Binding IsBusy}"
                               HorizontalOptions="Center"
                               VerticalOptions="Center"/>

        <Label HorizontalOptions="Center" FontSize="15" FontAttributes="Bold"
                   TextColor="{StaticResource Primary}" TextTransform="Uppercase">Editovat skupinu</Label>
        <Frame Margin="10" VerticalOptions="Center"  WidthRequest="300">


            <StackLayout>
                <Entry Placeholder="Jméno" Text="{Binding Band.Name}" Margin="10"></Entry>
                <Entry Placeholder="Sídlící v" Text="{Binding Band.BasedAt}" Margin="10"></Entry>
                <Entry Placeholder="Obrázek (URL)" Text="{Binding Band.Image}" Margin="10"></Entry>
                <Button Text="Uložit" Command="{Binding SaveCommand}"></Button>
            </StackLayout>

        </Frame>
    </StackLayout>
</ContentPage>